import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cron-job-manager-frontend',
  webDir: 'dist'
};

export default config;
